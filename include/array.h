// array.h
#ifndef ARRAY_H
#define ARRAY_H

#include <stdbool.h>

// Checks to see if the null terminated array contains the element.
bool array_contains(void *array, void *element);

#endif // ARRAY_H
